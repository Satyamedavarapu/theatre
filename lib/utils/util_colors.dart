import 'package:flutter/material.dart';

class UtilColors {
  static const Color appButton = Color.fromRGBO(251, 192, 45, 1);
  static const Color appBackground = Color.fromRGBO(10, 10, 10, 0.5);

  static const TextStyle buttonText = TextStyle(
    color: Colors.white,
    fontSize: 18.0,
  );
}
